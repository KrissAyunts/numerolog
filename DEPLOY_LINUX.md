# Инструкция по развертыванию

У вас есть два пути:
1. **GitHub Pages** — бесплатно, работает на серверах GitHub (Mini PC не нужен). Идеально для старта.
2. **Mini PC (Домашний сервер)** — бесплатно, работает на вашем железе. Нужен для полного контроля или если, как вы сказали, хотите задействовать Mini PC.

---

## Вариант 1: GitHub Pages (Самый простой) ☁️
Сайт будет работать на облаках GitHub. Вам не нужно платить за домен или хостинг.
Адрес будет вида: `ваше-имя.github.io/numerolog`

1. Зарегистрируйтесь на [GitHub.com](https://github.com/).
2. Создайте новый репозиторий (назовите его `numerolog`).
3. Загрузите туда файлы вашего проекта (папку `dist` или весь проект, настроив сборку).
   * **Простой способ:** Просто загрузите содержимое папки `dist` (index.html, assets) прямо в корень репозитория.
4. Зайдите в **Settings** (Настройки) репозитория -> **Pages**.
5. В разделе **Build and deployment** -> **Source** выберите `Deploy from a branch`.
6. **Branch**: `main` / `root` (корневая папка) -> Save.
7. Через пару минут сайт будет доступен по ссылке, которую покажет GitHub!

---

## Вариант 2: Mini PC + Бесплатный домен (DuckDNS) 🏠
Если вы хотите, чтобы сайт работал именно на **вашем Mini PC**, но платить за домен не хотите.

1. Зарегистрируйтесь на [DuckDNS.org](https://www.duckdns.org/) (вход через Google/GitHub).
2. Придумайте домен (например, `igor-numerolog`).
   * Ваш сайт будет: `http://igor-numerolog.duckdns.org`.
3. Нажмите **add domain**.

**Настройка на Mini PC:**
Теперь нужно, чтобы этот домен "смотрел" на ваш дом.
1. Следуйте инструкции на DuckDNS (раздел **install** -> **linux cron**).
   * Там будет скрипт, который сообщает DuckDNS ваш текущий IP.
2. **Важно:** Вам все равно придется открыть порт 80 или 3000 на роутере (Port Forwarding), как описано ранее, так как Cloudflare Tunnel требует своего домена (обычно), а для DuckDNS нужен прямой доступ.

---

## Вариант 3: Linux Mini PC (Сборка и Запуск) 🐧
*Если вы выбрали путь Mini PC, вот как его настроить.*

### Шаг 1: Подготовка
1. Установите Node.js:
   ```bash
   sudo apt update && sudo apt install nodejs npm -y
   ```

### Шаг 2: Сборка и Запуск
1. Скопируйте проект на сервер.
2. Соберите сайт:
   ```bash
   npm install
   npm run build
   ```
3. Запустите через PM2 (чтобы работал вечно):
   ```bash
   sudo npm install -g pm2
   pm2 serve dist 3000 --spa --name "numerolog"
   pm2 startup && pm2 save
   ```

### Шаг 3: Безопасность (Если открываете порты)
Если используете DuckDNS и открываете порты на роутере:
1. **UFW:** Разрешите порт 3000 (или 80).
   ```bash
   sudo ufw allow 3000
   ```
2. **Fail2Ban:** Обязательно поставьте, чтобы не взломали SSH.
